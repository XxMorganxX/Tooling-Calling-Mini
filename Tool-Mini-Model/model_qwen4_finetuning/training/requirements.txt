# Training Dependencies
# Install: pip install -r requirements.txt
#
# Prerequisites:
#   - CUDA toolkit (11.8 or 12.x)
#   - Python 3.10+
#
# Note: Install unsloth FIRST as it manages torch/transformers compatibility.
# See https://docs.unsloth.ai/get-started/install-and-update/pip-install

# ── Core training framework ─────────────────────────────────────────────
unsloth>=2026.2.1
trl>=0.27.1
transformers>=5.1.0
datasets>=3.0.0
peft>=0.14.0

# ── Quantization & optimization ─────────────────────────────────────────
bitsandbytes>=0.45.0
accelerate>=1.3.0

# ── Config parsing ──────────────────────────────────────────────────────
pyyaml>=6.0
sentencepiece>=0.2.0
